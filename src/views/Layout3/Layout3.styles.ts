import {StyleSheet} from 'react-native';
import theme from '../../theme';

const Layout3Styles = StyleSheet.create({
  container: {backgroundColor: theme.colors.BG_COLOR_VIEWS},
});

export default Layout3Styles;

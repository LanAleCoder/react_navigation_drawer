import {StyleSheet} from 'react-native';
import theme from '../../theme';

const Layout1Styles = StyleSheet.create({
  container: {
    backgroundColor: theme.colors.BG_COLOR_VIEWS,
    flex: 1,
    justifyContent: 'center',
  },
});

export default Layout1Styles;
